import express from 'express';

const router = express.Router();

// import * as stealController from '../controllers/steal.controller.js';

// router.route('/').get(stealController.get);
// router.route('/joke').get(stealController.joke);

export { router };
